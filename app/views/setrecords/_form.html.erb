<%= form_for @setrecord, :html => { :class => "form-horizontal setrecord" } do |f| %>

    <% if @setrecord.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@setrecord.errors.count, "error") %> prohibited this setrecord from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @setrecord.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>



  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            setrecords_path, :class => 'btn btn-default' %>

<% end %>
